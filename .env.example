# Required for lead notification email (website audit, submit_lead, MCP consult)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
LEAD_NOTIFICATION_EMAIL=

# Set true for implicit TLS (e.g. port 465). Port 465 also enables SSL without this.
# SMTP_USE_SSL=true

MCP_BASE_URL=http://localhost:8000
APP_ENV=development
# LOG_LEVEL=INFO
